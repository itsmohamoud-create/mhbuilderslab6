# OPTIONAL — the site and AI chat work perfectly without this.
# Add your key here only when you want the AI Assistant to use
# live Claude responses instead of the built-in smart fallback replies.
ANTHROPIC_API_KEY=
